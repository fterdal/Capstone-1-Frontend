import React, { useState } from "react";
import { useNavigate } from "react-router-dom";

const ViewResultsPage = () => {

    return (
        <div className="view-results-page">
            <h2>This is the poll results view page</h2>
        </div>
    );
};

export default ViewResultsPage;